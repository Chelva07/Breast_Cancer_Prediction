# Breast_Cancer_Prediction
Uses pretrained CNNs (ResNet50, EfficientNetB0) to extract features from the BreaKHis dataset, followed by ML models (SVM, RF, KNN) for classifying benign and malignant images.  Pipeline:  Preprocessing and splitting Feature extraction Model training and evaluation Best model selection
